wim_dir = join_paths(moduledir, 'utils', 'wimlib')
wim_sources = files([
    '__init__.py',
    'wimlib.py',
    'windows_edition.py',
    'wim_info.py'
])

install_data(wim_sources, install_dir: wim_dir)
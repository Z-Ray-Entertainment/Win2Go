bcdboot_dir = join_paths(moduledir, 'utils', 'bcdboot')
bcdboot_sources = files([
    '__init__.py',
    'bcdboot.py',
    'boot_reg.py',
    'hivex.py'
])

install_data(bcdboot_sources, install_dir: bcdboot_dir)
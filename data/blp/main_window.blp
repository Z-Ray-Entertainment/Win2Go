using Gtk 4.0;

template $Win2GoMainWindow: ApplicationWindow {
  default-width: 600;
  default-height: 300;
  title: "Win2Go";

  [titlebar]
  HeaderBar {}

  Label {
    label: bind template.main_text;
  }
}